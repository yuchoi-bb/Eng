# AndroidX 필수 — 없으면 :app이 설정 단계에서 실패한다.
android.useAndroidX=true

org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

kotlin.code.style=official
